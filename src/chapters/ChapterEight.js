import React from 'react';
import { ChapterHeader } from "../components.Components";
import { Divider, Grid, Header, Image, List, Segment } from "semantic-ui-react";

class ChapterEight extends React.Component {
    render() {
        return (
            <div className="scrollable justified">
                <ChapterHeader text="Chapter Eight" />
            </div>
        );
    }
}

export default ChapterEight;